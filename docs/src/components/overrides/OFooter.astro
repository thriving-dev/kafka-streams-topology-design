---
import type { Props } from '@astrojs/starlight/props';
import Default from '@astrojs/starlight/components/Footer.astro';
const isNonDocPage = ['', 'about'].includes(Astro.locals.starlightRoute.id);
console.log('id', Astro.locals.starlightRoute.id)
---

{ !isNonDocPage && <Default {...Astro.props}><slot /></Default> }
